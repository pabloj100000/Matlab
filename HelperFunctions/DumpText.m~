function DumpText(fileName, fid)
    fid2 = fopen(fileName,'r'); 

    tline = fgetl(fid2);
    while ischar(tline)
        fprintf(fid, [tline, '\n']);
        disp(tline)
        tline = fgetl(fid);
    end
    
    fclose(fid2);
end

